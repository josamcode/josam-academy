/**
 * PH-0.1 placeholder so the workspace builds.
 * The NestJS application, its module graph, and the health endpoint arrive at PH-0.3,
 * which also runs the DI probe that proves emitDecoratorMetadata survives the build.
 */
export const WORKSPACE_NAME = '@josam/api';
